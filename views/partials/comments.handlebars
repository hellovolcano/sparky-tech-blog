<div class="comments">
    <h2>Comments</h2>
  {{#each this}}
  <section class="comment">
    <div>
      {{comment_body}}
    </div>
    <div>
      --{{user.username}}, {{format_date created_at}}
    </div>
  </section>
  {{/each}}
</div>