<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="meta">
      {{user.username}} on {{created_at}}
    </div>
    <div>
      {{comment_body}}
    </div>
  </section>
  {{/each}}
</div>